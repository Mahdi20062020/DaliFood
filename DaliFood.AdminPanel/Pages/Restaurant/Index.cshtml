@page
@model DaliFood.AdminPanel.Pages.Restaurant.IndexModel
@{
}
<div class="container">
<a class="btn btn-outline-primary m-2" asp-page="upsert">افزودن رستوران جدید</a>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">نام</th>
                <th scope="col">#</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Restaurant)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>
                    <a asp-page="upsert" asp-route-Id="@item.Id" class="btn btn-warning">ویرایش</a>
                    <a asp-page-handler="Delete" asp-route-Id="@item.Id" onclick="return confirm('آیا از حذف این دسته بندی مطمئن هستید؟');" class="btn btn-danger">حذف</a>
                </td>
                </tr>
            }
        </tbody>
    </table>
</div>